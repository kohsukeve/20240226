import ModalBase from "./ModalBase";
import DatasetIcon from "@mui/icons-material/Dataset";

const Modal = () => {
  return <ModalBase title="HOME" content="NOMINEES" icon={<DatasetIcon />} />;
};

export default Modal;
